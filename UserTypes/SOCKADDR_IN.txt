typedef struct sockaddr_in {
    short   sin_family;    
    USHORT sin_port;
    IN_ADDR sin_addr;
    BYTE sin_zero[8]; // avoid CHAR sin_zero[8] as it will be interpreted as string
} SOCKADDR_IN, *PSOCKADDR_IN;
